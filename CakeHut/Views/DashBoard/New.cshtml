
    <!-- Sidebar -->
    <div class="sidebar">
        <a href="#" class="active"><span class="icon">🔲</span> Dashboard</a>
        <a href="#"><span class="icon">🗓</span> Reviews</a>
        <a href="#"><span class="icon">🗓</span> Tasks</a>
        <a href="#"><span class="icon">🗓</span> Manifest</a>
        <a href="#"><span class="icon">🗓</span> Invoices</a>
        <a href="#"><span class="icon">🗓</span> Skills</a>
        <a href="#"><span class="icon">🖌</span> Appearance</a>
        <a href="#"><span class="icon">↩️</span> Logout</a>

        <div class="sidebar-footer">
            <a href="#"><span class="icon">👤</span> Selma B</a>
        </div>
    </div>

    <!-- Dashboard Content -->
    <div class="dashboard-content">
        <h1>Welcome to Dashboard</h1>
    </div>


<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        display: flex;
        height: 100vh;
    }

    /* Sidebar */
    .sidebar {
        position: fixed;
        left: 0;
        top: 0;
        height: 100%;
        width: 250px;
        background-color: #f8f9fa;
        box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
    }

        .sidebar a {
            display: block;
            padding: 15px 20px;
            color: #333;
            text-decoration: none;
            font-size: 16px;
            border-bottom: 1px solid #eaeaea;
            transition: background 0.3s;
        }

            .sidebar a:hover {
                background-color: #eaeaea;
            }

            .sidebar a.active {
                background-color: #4a60f6;
                color: white;
            }

    /* Content Area */
    .content {
        margin-left: 250px; /* Matches the sidebar width */
        padding: 20px;
        width: calc(100% - 250px); /* Full width minus sidebar */
    }

        .content h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }
</style>
